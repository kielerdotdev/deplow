{
	auto_https off
	# admin used by `caddy reload` from the control plane after route changes
	admin 127.0.0.1:2019
}
:80 {
	handle /hostrig-health {
		respond "ok" 200
	}
	import /etc/caddy/routes/*.caddy
	respond "hostrig proxy — no matching project route" 404
}
