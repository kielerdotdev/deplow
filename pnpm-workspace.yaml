packages:
  - "apps/*"
  - "packages/*"

onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
  - sharp
  - unrs-resolver

overrides:
  # Pin Vitest to the version bundled with vite-plus (vp test)
  vitest: "4.1.10"
  drizzle-orm: 0.45.2
