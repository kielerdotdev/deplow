# SQLite — relative paths resolve from packages/db (not this app's cwd)
DATABASE_URL=data/hostrig.db

# Better Auth — use a long random string in production
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:9565

# Secrets encryption (can match BETTER_AUTH_SECRET in dev)
HOSTRIG_SECRETS_KEY=

# PITR (pgBackRest stanza = project id)
# HOSTRIG_PITR_ENABLED=1
# PGBACKREST_CONFIG=/absolute/path/to/infra/pgbackrest/pgbackrest.conf
# HOSTRIG_PGBACKREST_IMAGE=woblerr/pgbackrest:2.58.0-alpine

# gVisor RuntimeClass for user app pods (always required; runc not allowed)
HOSTRIG_APP_RUNTIME=runsc
HOSTRIG_APP_RUNTIME_REQUIRED=true
# HOSTRIG_APP_MEMORY_MB=512
# HOSTRIG_APP_CPUS=1
# HOSTRIG_APP_READONLY_ROOTFS=true

# Public URL / proxy — local defaults seed the DB on first boot only.
# After that, configure Domains in the app (Domains tab).
# Edges forward to http://caddy:80 (compose) or http://127.0.0.1:8088 (host).
HOSTRIG_BASE_DOMAIN=apps.localhost
HOSTRIG_PUBLIC_URL_PROTOCOL=http
HOSTRIG_PROXY_ROUTES_DIR=infra/caddy/routes
# Control plane public URL (OAuth, webhooks, MCP)
HOSTRIG_PUBLIC_URL=http://localhost:9565
# Client-side only: Bearer token from Settings → MCP (see docs/mcp.md)
# HOSTRIG_MCP_TOKEN=hostrig_...

# Optional. Leave unset — Dev Container / production image put railpack on PATH.
# RAILPACK_BIN=/usr/local/bin/railpack

# Optional: spawn agent VMs on Hetzner Cloud (Settings → Nodes).
# HOSTRIG_PUBLIC_URL must be reachable from the Internet (tunnel) for join.
# HOSTRIG_HETZNER_API_TOKEN=
# HOSTRIG_HETZNER_LOCATION=fsn1
# HOSTRIG_HETZNER_SERVER_TYPE=cpx22
# HOSTRIG_HETZNER_IMAGE=ubuntu-24.04
# HOSTRIG_HETZNER_SSH_KEYS=hostrig-devvm
# Danger: unlocks vitobotta/hetzner-k3s (micro only). Token alone is not enough.
