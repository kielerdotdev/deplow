# Implementation Plans

Generated by the improve skill on 2026-07-12; extended 2026-07-12 with
`shadcn/ui@shadcn` + `emilkowalski/skills@review-animations` review pass.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Unifyify overlay motion and add reduced-motion + page enter animations | P1 | M | — | DONE |
| —    | Follow-up: review-animations + shadcn craft fixes (in-tree, not a numbered plan) | P1 | M | 001 | DONE |

## Review notes (001)

- **Verdict**: APPROVE (done criteria pass; structure test 18/18).
- **Branch / commit**: `advisor/001-unify-ui-motion` @ `89687b1`.
- **Caveat**: executor ran on the dirty main checkout (not a clean isolated worktree). Commit `89687b1` therefore includes prior uncommitted WIP already present in the in-scope files (org shell, styles tokens, new `popover.tsx`), not a motion-only delta. Motion edits themselves match the plan.
- Merging / pushing is left to the operator.

## Follow-up (shadcn + review-animations) — applied in working tree

See conversation findings table. Key landings:

- Command palette: `DialogContent animated={false}` (keyboard / high-frequency — no open motion)
- Strong `--ease-out-ui` / `--ease-drawer` tokens; overlays use `ease-out-ui`
- Sheet: `transition-[opacity,transform]` (no unbounded `transition`)
- Button: drop `transition-all`; press uses `scale-[0.98]`
- Stack cards: drop hover translate + `transition-all`
- EmptyState composes shadcn `Empty`; installed `spinner`
- SettingsSection: `space-y-*` → `flex flex-col gap-*`

## Dependency notes

- Single numbered plan; follow-up builds on 001.

## Findings considered and rejected

- Adding Framer Motion app-wide: rejected — repo already uses `tw-animate-css` + Base UI.
- Staggered list animations on Home/project cards: rejected — noise vs polish.
- Animating `apps/site` marketing pages: out of scope for this control-plane pass.
- Migrating every form to Field/FieldGroup in one pass: deferred — too large; SettingsField gap fix is the shared foothold.
- Adding sonner toasts app-wide: deferred — no toast usage yet; install when first toast lands.
